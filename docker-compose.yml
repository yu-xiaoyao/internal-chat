services:
  chat-nginx:
    image: nginx:latest
    container_name: chat-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginxvhost.conf:/etc/nginx/conf.d/default.conf
      - ./www:/var/www